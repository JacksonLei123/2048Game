let keypress,Game;_b85‍.w("keypress",[["default",["keypress"],function(v){keypress=v}]]);_b85‍.w("./engine/game",[["default",["Game"],function(v){Game=v}]]);


keypress(process.stdin);

/**
 * The code in this file is used to run your game in the console. Use it
 * to help develop your game engine.
 *
 */

let game = new Game(4);
_b85‍.g.console.log(game.toString());
// game.loadGame({
//     board: [ 0, 0, 8, 0,
//         0, 0, 0 ,0,
//         0 ,0 ,0 ,8,
//         8 ,8 ,8, 0],
//     score: 0,
//     won: false,
//     over: false
//   });

game.onMove(gameState => {
    _b85‍.g.console.log(game.toString());
    
});

game.onWin(gameState => {
    _b85‍.g.console.log('You won with a gameState of...', gameState)
});

game.onLose(gameState => {
    _b85‍.g.console.log('You lost! :(', gameState)
    console.log(`Your score was ${gameState.score}`);
});

process.stdin.on('keypress', function (ch, key) {
    switch (key.name) {
        case 'right':
            game.move('right');
            break;
        case 'left':
            game.move('left');

            break;
        case 'down':
            game.move('down');

            break;
        case 'up':
            game.move('up');
            break;
    }
    if (key && key.ctrl && key.name == 'c') {
        process.stdin.pause();
    }
});


process.stdin.setRawMode(true);
process.stdin.resume();

